# Copy to .env.local for local dev, or configure in your deploy platform
# (Vercel → Project Settings → Environment Variables).
#
# Vite inlines variables prefixed with VITE_ at BUILD TIME. If you change this
# for a deployed build, you must redeploy — it is not a runtime variable.

VITE_API_BASE=http://localhost:8000